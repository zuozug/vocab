# 阶段二后端初始化记录

## 1. 完成时间

2026-04-21

## 2. 技术栈

- Python：`3.14.3`
- Web 框架：`FastAPI`
- ASGI 服务：`Uvicorn`
- 配置管理：`pydantic-settings`
- PostgreSQL 驱动：`psycopg`
- 测试：`pytest`
- Lint：`ruff`

---

## 3. 已创建内容

后端目录：[backend](backend)

核心文件：

- [backend/pyproject.toml](backend/pyproject.toml)
- [backend/.env.example](backend/.env.example)
- [backend/app/main.py](backend/app/main.py)
- [backend/app/core/config.py](backend/app/core/config.py)
- [backend/app/core/database.py](backend/app/core/database.py)
- [backend/app/api/routes/health.py](backend/app/api/routes/health.py)
- [backend/tests/test_health.py](backend/tests/test_health.py)

本地开发文件：

- `backend/.env`
- `backend/.venv`

以上两个路径已被 `.gitignore` 忽略。

---

## 4. 当前接口

- `GET /health`
  - 用于检查 API 服务是否可用。
- `GET /health/db`
  - 用于检查 API 是否可以连接 PostgreSQL。

FastAPI 自动文档：

- `/docs`
- `/redoc`

---

## 5. 数据库连接

当前开发环境通过 `backend/.env` 读取数据库连接：

```env
APP_ENV=development
DATABASE_URL=postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/DB_NAME
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
```

真实密码只保存在本地 `backend/.env`，不写入项目文档和示例配置。

---

## 6. 验证结果

应用导入验证：

- 应用标题：`Vocab API`
- 当前路由：
  - `/openapi.json`
  - `/docs`
  - `/docs/oauth2-redirect`
  - `/redoc`
  - `/health`
  - `/health/db`

数据库连接验证：

- 已成功连接 PostgreSQL `17.9 (Debian 17.9-0+deb13u1)`。

测试结果：

```text
1 passed
```

Lint 结果：

```text
All checks passed
```

---

## 7. 启动方式

进入后端目录：

```powershell
cd backend
```

启动服务：

```powershell
.\.venv\Scripts\python -m uvicorn app.main:app --reload
```

访问：

- `http://127.0.0.1:8000/health`
- `http://127.0.0.1:8000/health/db`
- `http://127.0.0.1:8000/docs`

---

## 8. 阶段二结论

阶段二已完成：

- FastAPI 后端项目已初始化。
- 配置与代码已分离。
- 数据库连接模块已建立。
- CORS 已配置为支持后续 Vite 本地前端。
- 健康检查接口已实现。
- 基础测试与 lint 已通过。

下一步可以进入阶段三：单词管理 API。
