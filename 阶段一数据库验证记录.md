# 阶段一数据库验证记录

## 1. 验证时间

2026-04-21

## 2. 数据库信息

- 数据库：`DB_NAME`
- 地址：`DB_HOST`
- 用户：`DB_USER`
- PostgreSQL 版本：`17.9 (Debian 17.9-0+deb13u1)`

密码不写入项目文件。

---

## 3. 本次操作

已按开发库处理，清空并重新初始化当前项目对象。

删除对象：

- `meaning`
- `word_sense`
- `review_state`
- `study_session`
- `word`
- `set_updated_at()`
- `part_of_speech_enum`

随后重新执行：[schema.sql](schema.sql)

---

## 4. Schema 验证结果

以下对象均已重新创建并验证存在：

- 类型：`part_of_speech_enum`
- 表：`word`
- 表：`word_sense`
- 表：`meaning`
- 表：`review_state`
- 表：`study_session`
- 函数：`set_updated_at`
- 触发器：`trg_word_updated_at`
- 触发器：`trg_word_sense_updated_at`
- 触发器：`trg_meaning_updated_at`
- 触发器：`trg_review_state_updated_at`

---

## 5. 测试数据

已写入测试单词 `record`：

- `record`
  - `n`
    - `唱片`
    - `记录`
  - `v`
    - `录制`
    - `记录`

当前表数据量：

- `word`：1
- `word_sense`：2
- `meaning`：4
- `review_state`：1
- `study_session`：0

---

## 6. 阶段一结论

阶段一已完成：

- 服务器数据库可连接。
- PostgreSQL 版本已确认。
- 当前 schema 可从空状态重新初始化。
- 多词性、多释义结构可正常写入和查询。
- `review_state` 可与 `word` 建立一对一记录。

下一步可以开始阶段二：FastAPI 后端项目初始化。
